## VGP EDD Stats - Configuration
#
# Copy to `scripts/.env` and fill in values. `scripts/.env` is gitignored.

# =============================================================================
# SSH TUNNEL CONFIGURATION
# =============================================================================
# NOTE: SSH port forwarding must be enabled on the remote server.
# If you get "administratively prohibited" errors, the host has disabled
# port forwarding. Contact your hosting provider or use an alternative method.
# =============================================================================

# SSH destination for the live server
LIVE_SSH="user@hostname.com"

# Local port for the SSH tunnel (default: 3307)
TUNNEL_LOCAL_PORT="3307"

# Remote MySQL host/port (as seen from the SSH server)
# Use 127.0.0.1 instead of localhost to force TCP
REMOTE_MYSQL_HOST="127.0.0.1"
REMOTE_MYSQL_PORT="3306"
