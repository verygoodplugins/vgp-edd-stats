## VGP EDD Stats - Live Data Sync Configuration
#
# Copy to `scripts/.env` and fill in values. `scripts/.env` is gitignored.

# SSH destination for the live server.
LIVE_SSH="user@host"

# Absolute path to the live WordPress install on the server.
LIVE_WP_PATH="/home/master/applications/urjxzpmdrd/public_html"

# Live database table prefix (usually `wp_`).
LIVE_DB_PREFIX="wp_"

# Local MySQL connection info.
#
# If you're using LocalWP, the script will auto-detect the local DB from
# Local's `sites.json` + socket in the Local run directory.
# Set to "0" to force the manual LOCAL_DB_* overrides below.
AUTO_DETECT_LOCAL_DB="1"

# Optional: override which mysql client binary to use.
# Useful if your system `mysql` can't connect to Local's mysqld due to auth plugins.
# LOCAL_MYSQL_BIN="/absolute/path/to/mysql"

# Optional: point directly at a wp-config.php if your plugin path is unusual.
# LOCAL_WP_CONFIG="/absolute/path/to/wp-config.php"

# Optional overrides (uncomment to force).
# LOCAL_DB_HOST="127.0.0.1"
# LOCAL_DB_PORT="10000"
# LOCAL_DB_SOCKET="/path/to/mysql.sock"
# LOCAL_DB_USER="root"
# LOCAL_DB_PASS="root"

# Local development database to create/overwrite.
# IMPORTANT: This must be DIFFERENT from WordPress's database ('local').
# The sync script will DROP and recreate this database!
LOCAL_DEV_DB="vgp_edd_dev"
